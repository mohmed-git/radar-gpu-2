{
  "name": "gpu-radar",
  "version": "2.0.0",
  "description": "GPU Radar — تتبع أسعار قطع الكمبيوتر يومياً — بدون API مدفوع",
  "main": "api/server.js",
  "scripts": {
    "start":  "node api/server.js",
    "dev":    "nodemon api/server.js",
    "update": "node -e \"require('./api/jobs/dailyUpdate').runDailyUpdate().then(r=>console.log('Done:',r)).catch(console.error)\""
  },
  "dependencies": {
    "cors":       "^2.8.5",
    "dotenv":     "^16.4.5",
    "express":    "^4.18.2",
    "jsdom":      "^24.0.0",
    "node-cron":  "^3.0.3",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": { "node": ">=18.0.0" },
  "license": "MIT"
}
